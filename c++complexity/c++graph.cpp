//c++graph.cpp william k. johnson 2015

#include <c++complexity.h>

using namespace cpp_complexity;

int main( int argc , char* argv[] )
{
    /*edge g = std::make_tuple( "foo" , "jim" );
    edge r = std::make_tuple( "jim" , "bar" );
    //
    vertex m( "foo" );
    vertex s( "bar" );
    vertex k( "jim" );
    //graph g;
    std::vector<edge> x;
    x.push_back( g );
    x.push_back( r );
    std::vector<vertex> v;
    v.push_back( m );
    v.push_back( s );
    v.push_back( k );
    cc_graph ccg( &v , &x);
    std::pair<bool,edge> pr = ccg.retr_edge( k , s );
    std::cout << pr.second << "\n";
    vertices vs = ccg.enum_vertices();
    std::vector<edge> se;
    se = ccg.enum_edges();*/

    return ( 0 );
}
